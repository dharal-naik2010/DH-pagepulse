{
  "name": "pagepulse-backend",
  "version": "1.0.0",
  "description": "Page Pulse API — audits any URL and returns a JSON health report",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "nodemon": "^3.0.2"
  }
}
